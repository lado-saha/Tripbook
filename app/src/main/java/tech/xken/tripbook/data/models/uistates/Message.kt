package tech.xken.tripbook.data.models.uistates

/**
 * A class to handle and show different errors and warning and snackbars to the user
 */
data class Message(
    val id: Long,
    val message: Message
    //val action: () -> Unit
) {
}